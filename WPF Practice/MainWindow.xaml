<Window x:Class="WPF_Practice.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF_Practice"
        mc:Ignorable="d"
        Title="MainWindow" Height="190" Width="250">
    <StackPanel Margin="10">
        <Label Target="{Binding ElementName=txtName}">
            <StackPanel Orientation="Horizontal">
                <Image Source="/icons/user.png" Width="16" Margin="0,0,5,0"/>
                <AccessText Text="_Name"/>
            </StackPanel>
        </Label>
        <TextBox Name="txtName"/>
        <Label Target="{Binding ElementName=txtPassword}">
            <StackPanel Orientation="Horizontal">
                <Image Source="/icons/lock.png" Width="16" Margin="0,0,5,0"/>
                <AccessText Text="_Password"/>
            </StackPanel>
        </Label>
        <TextBox Name="txtPassword"/>
    </StackPanel>
</Window>
